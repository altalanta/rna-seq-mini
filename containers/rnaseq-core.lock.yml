name: rnaseq-mini-core
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # Core Python tools (from base.yml)
  - python=3.11
  - snakemake>=7
  - nextflow>=24
  - mamba
  - pip
  - ruff
  - black
  - yamllint
  - pandas
  - pyyaml
  - rich
  - click
  - pytest
  - pre-commit
  - schema

  # QC tools (from qc.yml)
  - fastqc
  - multiqc
  - pigz

  # Quantification tools (from salmon.yml)
  - salmon>=1.10
  - samtools
  - seqkit

  # Utility tools (from base.yml)
  - git
  - wget
  - curl
  - unzip

  # Pip dependencies (from base.yml)
  - pip:
      - snakemake-executor-plugin-slurm
