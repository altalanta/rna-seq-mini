profiles {
  local {
    process.executor = 'local'
    process.cpus = 4
    process.memory = '8 GB'
    process.errorStrategy = 'retry'
    process.maxRetries = 1
    conda.enabled = true
    conda.cacheDir = '.conda'
    publishDir = [
      path: 'results',
      mode: 'copy'
    ]
  }
}
